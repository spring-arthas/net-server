# 局域网明文 TCP

## 目标

- 局域网测试默认不启用 TLS 或加载证书。
- NIO 与媒体端口直接允许同一局域网内的客户端访问。
- 媒体链接使用 HTTP。

## 验收

- `TLS.GATEWAY.ENABLED=false`。
- `10086`、`10087`、`10088`、`10188` 由 Java 明文服务监听局域网地址。
- 相关配置测试与 Maven 构建通过。
